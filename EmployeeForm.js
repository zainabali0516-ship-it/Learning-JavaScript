import { useState, useEffect } from "react";
export default function EmployeeForm({ addEmployee, editingEmployee }) { const empty = { name: "", email: "", position: "", department: "", salary: "" }; const [e, setE] = useState(empty); useEffect(() => { if (editingEmployee) setE(editingEmployee) }, [editingEmployee]); return <form className="employee-form" onSubmit={ev => { ev.preventDefault(); addEmployee(e); setE(empty); }}>{["name", "email", "position", "department", "salary"].map(f => <input key={f} name={f} type={f == "email" ? "email" : "text"} placeholder={f} value={e[f]} onChange={x => setE({ ...e, [f]: x.target.value })} />)}<button>{editingEmployee ? "Update" : "Add"} Employee</button></form> }